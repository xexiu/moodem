import { ViewStyle } from 'react-native';

export const commonFormContainer = {
    borderWidth: 1,
    fontStyle: 'italic',
    padding: 5,
    borderColor: '#eee',
    borderRadius: 5,
    width: '80%'
} as ViewStyle;
