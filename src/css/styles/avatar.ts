import { ViewStyle } from 'react-native';
export const avatarContainer = {
    alignSelf: 'center',
    marginBottom: 10,
    alignItems: 'center'
} as ViewStyle;

export const avatarImage = {
    width: 90,
    height: 90,
    borderColor: '#eee',
    borderWidth: 1,
    borderRadius: 10
};
